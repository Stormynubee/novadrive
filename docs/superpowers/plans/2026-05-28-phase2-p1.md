# Margi Phase 2 P1 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Ship v1.6.0 drive HUD redesign, then v1.7.0 Rah-Veer, offline trip cards, journey debrief/history, and FSM TTS narrator.

**Architecture:** Extract driving HUD components with layout spec (TDD); extend SQLite/AsyncStorage for claims, summaries, hazards; wire narrator on START triage screen.

**Tech Stack:** Expo SDK 54, expo-router, expo-sqlite, AsyncStorage, expo-speech, Jest.

**Design spec:** [../specs/2026-05-28-phase2-p1-design.md](../specs/2026-05-28-phase2-p1-design.md)

---

### Task 1: HUD layout spec (TDD)

- [x] `src/lib/driving/hudLayoutSpec.ts` + `hudLayoutSpec.test.ts`
- [x] `DrivingHudLayout`, `CompactSpeedometer`, `SensorStatusRow`, `DrivingHudHeader`
- [x] Refactor `app/journey.tsx`; `HoldSOSButton` `hudTop` variant
- [x] `DESIGN.md` + `DEVICE_SMOKE_MATRIX.md`

### Task 2: Rah-Veer + QR

- [x] `packetFromQrDecoded` in `ghp.ts`
- [x] `relayChain.ts`, `rahveerDb.ts` + tests
- [x] `app/rahveer/*`, scan + relay entry points

### Task 3: Offline cards + cache

- [x] `tripBriefingDb.ts`, `tripPlanCache.ts` + tests
- [x] Briefing section on `PlanCorridorScreen`

### Task 4: Journey debrief

- [x] `summary_json` migration in `journeyDb.ts`
- [x] Scroll sections on `journey/complete.tsx`
- [x] `settings/journey-history.tsx`

### Task 5: TTS narrator

- [x] `src/lib/tts/narrator.ts` + test
- [x] `app/emergency/triage.tsx` START FSM UI + speak hooks

### Task 6: Release

- [x] Version 1.7.0, CHANGELOG, VERSION_HISTORY, tags, push
