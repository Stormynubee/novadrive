import { PlanCorridorScreen } from '../../src/components/PlanCorridorScreen';

/** Trip tab — Stitch `plan_corridor_route_planning` (shared PlanCorridorScreen). */
export default function DriveTabScreen() {
  return <PlanCorridorScreen />;
}
